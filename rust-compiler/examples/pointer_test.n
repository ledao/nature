// pointer_test.n - Test pointer support
import fmt

fn main() {
    var x: int = 42
    var p: int* = &x  // address of operation
    var y: int = *p   // dereference operation
    
    fmt.printf("x = %d\n", x)
    fmt.printf("p = %p\n", p)  // print pointer value
    fmt.printf("y = %d\n", y)
}
